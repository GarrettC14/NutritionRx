# NutritionRx Environment Variables
# Copy this file to .env.local and fill in your values

# Sentry DSN for crash reporting (from Sentry project Settings → Client Keys)
# Format: https://xxx@xxx.ingest.sentry.io/xxx
EXPO_PUBLIC_SENTRY_DSN=

# Supabase Edge Functions base URL (backend proxy + referrals)
EXPO_PUBLIC_SUPABASE_FUNCTIONS_URL=

# RevenueCat API Keys (from RevenueCat dashboard → Project → API Keys)
# iOS key format: appl_xxxxxxxxxxxxx
# Android key format: goog_xxxxxxxxxxxxx
EXPO_PUBLIC_REVENUECAT_IOS_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_KEY=

# OpenAI API key is managed server-side in Supabase secrets (OPENAI_API_KEY_NUTRITIONRX).
# It is NOT bundled into the app.

# NOTE: SENTRY_AUTH_TOKEN is a build-time secret for source map uploads.
# Do NOT add it here. Set it via: eas secret:create --name SENTRY_AUTH_TOKEN
