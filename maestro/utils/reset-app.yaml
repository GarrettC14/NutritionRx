appId: com.nutritionrx.app
name: Reset App to Fresh State
---
# Relaunch app with cleared state for a clean slate
- launchApp:
    clearState: true

- extendedWaitUntil:
    visible:
      id: "legal-screen"
    timeout: 10000
